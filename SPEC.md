# kakeibo アプリ 仕様書（AI引き継ぎ用）

このファイルは、このプロジェクトを別のAI（Claude / ChatGPT等）に引き継いで作業を続けてもらうための説明書です。
新しいAIとの会話を始めるときは、まずこのファイルの中身を貼り付けるか読み込ませてください。

## 目的

個人用の家計簿アプリ。これまでスプレッドシートに手入力していたが、面倒なのでやめたい。
クレジットカード等のCSV明細をアップロードするだけで、自動集計・グラフ表示されるダッシュボードを作る。

## 最重要の設計方針（絶対に変更しないこと）

特定のAIサービスに依存しない、という方針が最優先。理由：以前ChatGPTのProプランでプロジェクト機能に
大量のデータを溜め込んで運用していたところ、ある日そのプロジェクト内で新規会話ができなくなるという
不具合が発生し、データのエクスポートも困難で大きく困った経験があるため。

**【重要な注意】過去に、実際の金額データが入ったJSONファイルを誤ってGitHub（Public）にアップロードして
しまう事故が発生した。リポジトリを作り直して対処済みだが、二度と繰り返さないこと。
実データ（金額・利用先・個人名などが入ったCSV/JSON）は、絶対にGitHubリポジトリに入れてはいけない。
GitHubに置いてよいのはコード（index.html, SPEC.md, .gitignore）のみ。実データは必ずアプリの画面上
（ブラウザ）で「JSONファイルを選択」等から読み込む。**

これを踏まえ：
- アプリ本体はただの静的なHTML/CSS/JavaScript（ビルド不要、フレームワーク不使用）で作る
- コードはGitHubリポジトリ `kakeibo` で管理し、GitHub Pagesで公開する
- データ（家計簿の中身）はAI/クラウドサービス側には一切保存しない。ブラウザのlocalStorageに自動保存し、
  いつでもJSONファイルとして書き出し／読み込みできるようにする
- AIは「コードを書く手伝い」をするだけの存在。ある日Claudeが使えなくなっても、
  このリポジトリのコードとSPEC.mdさえあれば、別のAIや人間が続きを作業できる状態を保つ

## ユーザー環境

- 非エンジニア。ターミナル/コマンド操作は避け、GitHubのブラウザ画面での操作（ファイルのアップロード等）を基本とする
- GitHubアカウントあり。リポジトリ `kakeibo` を作成済み（Public、README付き）
- 今後の改修もAI（Claude等）に都度依頼する想定

## 支払い手段と明細フォーマット

| 支払い手段 | 用途 | CSV対応状況 |
|---|---|---|
| 三井住友ビジネスオーナーズゴールドカード | 国内メインのカード | 対応済み（下記フォーマット） |
| ANA nimocaカード（三井住友系） | 交通系IC、少額決済 | 対応済み（本カードと同一フォーマット） |
| ソニー銀行デビットカード | 外貨決済（海外旅行等） | **保留**。サイトにログインしても明細CSVが見つからず、取得方法を後日再検討 |
| 楽天カード | 今月の支払いで解約予定 | 対応不要（過去データのみ） |
| QRコード決済・現金 | 少額 | 優先度低。将来的に手動入力欄を用意するかもしれない |

### 三井住友カード系CSVフォーマット（本カード・ANA nimocaカード共通）

- 文字コード：Shift-JIS
- 改行：CRLF
- 1行目：`氏名, カード番号(マスク済み), カード名`（ヘッダーとして無視する）
- 2行目以降：`日付(YYYY/MM/DD), 利用先, 金額, 数量(常に１), 数量(常に１), 金額(再掲), 備考`
  - 金額が3列目に無く6列目にだけ入っているケースがある（例：年会費の行）。6列目を優先的に見る実装にする
- 最終行：`,,,,,合計金額,`（日付が空なので読み飛ばす）

### 過去の手入力データ（2026年1月〜8月、Excel）

- 255件、Claudeとの会話内で抽出しJSON化済み（ユーザーの手元にある想定。ファイル名: manual_data_2026_01-08.json）
- 項目：date, amount, item, payment_method_raw, merchant, old_category_code
- old_category_code は当時の独自ルールによる分類番号で、月ごとに厳密な意味が統一されていない可能性がある。
  新しいカテゴリ体系への自動マッピングはせず、キーワードベースの自動分類にかけ直す方針

## カテゴリ分類ルール

キーワードマッチによる自動分類＋手動修正のハイブリッド方式（ユーザー回答済み）。
初期カテゴリ案：食費・日用品・美容・学び／書籍・ファッション・医療費・交通・住居光熱・税金手数料・交際費・趣味娯楽・未分類。
各カテゴリにキーワードリストを持たせ、利用先名に部分一致したら自動割り当て。ユーザーが画面上で個別に修正可能にする。

## データ保存方式（ユーザー回答済み）

自動保存＋いつでも書き出しできる方式：
- ブラウザのlocalStorageに自動保存（アプリを開けば続きが見える）
- 「バックアップとして書き出す」ボタンで、いつでもJSONファイルとしてダウンロード可能にする
- 「読み込む」ボタンで、書き出したJSON（または手入力データ移行用JSON）を読み込めるようにする

## 見たい情報（ユーザー回答待ち／未確定）

以下は仮案。requirements確定時にここを更新すること：
- 月別の支出推移
- カテゴリ別の内訳
- カード／支払い方法別の内訳
- 楽天カード解約前後の比較

## 進捗ステータス

- [x] 要件整理、支払い手段の洗い出し
- [x] 三井住友系CSVフォーマットの解析
- [x] 過去データ（Excel）の抽出・JSON化
- [x] React版の試作（artifact上で動作確認、方針転換により破棄予定）
- [ ] 素のHTML/JS/CSSでアプリ本体を作り直す ← 次のタスク
- [ ] GitHubリポジトリへのアップロード手順の実施
- [ ] GitHub Pagesの有効化・公開URLの確認
- [ ] ソニー銀行デビットカードのCSV取得方法の再検討
- [ ] カテゴリ分類ルールの最終調整

## リポジトリ情報

- リポジトリ名：`kakeibo`（Public）
- 公開方法：GitHub Pages
